# Rust

Rust é uma linguagem de sistemas focada em **performance** e **segurança de memória**.

Rust não usa garbage collector. Rust usa ownership e borrowing para garantir
segurança em tempo de compilação. Muitos times de engenharia de dados estão
adotando Rust para pipelines críticos de performance.
