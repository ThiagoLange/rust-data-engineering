# Defina uma das chaves para chamadas reais (senão o pipeline usa embeddings e LLM simulados)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
# Custo: limite tokens por chamada (padrão 512), não rode loops ilimitados sem controle
